if (student.age >= 18 && student.age <= 25) {
    cout << student.name << " is eligible for case 1 (Age eligibility)." << endl;
} else {
    cout << student.name << " is not eligible for case 1 (Age eligibility)." << endl;
}
