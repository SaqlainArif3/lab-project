if (student.age >= 18 && student.age <= 25 && student.grade >= 60) {
    cout << student.name << " is eligible for case 4 (Age and grade eligibility)." << endl;
} else {
    cout << student.name << " is not eligible for case 4 (Age and grade eligibility)." << endl;
}
